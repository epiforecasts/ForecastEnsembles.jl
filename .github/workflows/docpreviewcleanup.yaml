# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Doc Preview Cleanup

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  doc-preview-cleanup:
    uses: EpiAware/.github/.github/workflows/docs-preview-cleanup.yml@0db77a6b64d4fcc27cbb4df37025a2080ea6cd9d
    secrets: inherit  # pragma: allowlist secret
