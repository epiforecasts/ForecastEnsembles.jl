# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Doc Preview Cleanup

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  doc-preview-cleanup:
    uses: EpiAware/.github/.github/workflows/docs-preview-cleanup.yml@11263ddd07f3d88c54a6245828a81d10a83fcb29
    secrets: inherit  # pragma: allowlist secret
